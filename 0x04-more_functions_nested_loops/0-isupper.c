#include "main.h"
#include <stdio.h>

/**
 * main - checks-for uppercase
 * @c: c is an ascii character
 *
 * Return: 1 if c is uppercase or 0 if lowercase
 */

int _isupper(int c)
{
        if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
        }
	
	return (isupper);
}	
