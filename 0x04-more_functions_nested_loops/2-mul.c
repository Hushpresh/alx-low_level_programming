#include "main.h"
#include <stdio.h>

/**
 * main - checks-for multipy
 * @c: c is an ascii characteer
 *
 * Return: product.
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
